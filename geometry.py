def AreaOfTriangle(breadth, height):
    return (breadth * height) / 2


def AreaOfSquare(side):
    return side * side


def AreaOfRectangle(breadth, length):
    return breadth * length


def AreaOfCircle(radius):
    pi = 3.14
    return pi * radius * radius
